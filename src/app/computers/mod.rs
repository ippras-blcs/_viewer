pub(in crate::app) use self::{
    // plot::resampling::{Computed as PlotResamplingComputed, Key as PlotResamplingKey},
    // plot::rolling::{Computed as RollingComputed, Key as RollingKey},
    // plot::source::{Computed as PlotSourceComputed, Key as PlotSourceKey},
    plot::{Computed as PlotComputed, Key as PlotKey},
    // table::{Computed as TableComputed, Key as TableKey},
};

mod plot;
// mod table;
